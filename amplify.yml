version: 1
frontend:
  phases:
    preBuild:
      commands: []
    build:
      commands: []
  artifacts:
    baseDirectory: ./
    files:
      - '**/*'  # Include all files and folders recursively
  cache:
    paths: []